{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "",
  "main": "./src/bot.js",
  "dependencies": {
    "cheerio": "^0.22.0",
    "nodemon": "^1.11.0",
    "request": "^2.75.0",
    "request-promise": "^4.2.1",
    "websocket": "^1.0.24",
    "ws": "^1.1.1"
  },
  "devDependencies": {},
  "scripts": {
    "start": "set PORT=7001 && set DEBUG=websocket:connection && node ./src/bot.js",
    "refresh": "set PORT=7001 && set DEBUG=websocket:connection && nodemon -e js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tim Porritt",
  "license": "ISC"
}
